# Task 1 (original prompt)
# Generate a 5-zone building model with identical rectangular zones sharing adjoining east-west walls. Each zone measures 7.0 m (length) × 4.9 m (width) × 7.5 m (height), featuring 3.0 m (length) × 1.2 m (width) windows on both south and north walls with west edges offset 0.4 m from wall west edges, both having 1.25 m sill height.
#
# extension1-1
# Generate a 7-zone building model with identical rectangular zones sharing adjoining east-west walls. Each zone measures 7.0 m (length) × 4.9 m (width) × 7.5 m (height), featuring 3.0 m (length) × 1.2 m (width) windows on both south and north walls with west edges offset 0.4 m from wall west edges, both having 1.25 m sill height.
#
# extension 1-2
# Generate a 3-zone building model with identical rectangular zones sharing adjoining east-west walls. Each zone measures 7.0 m (length) × 4.9 m (width) × 7.5 m (height), featuring 3.0 m (length) × 1.2 m (width) windows on both south and north walls with west edges offset 0.4 m from wall west edges, both having 1.25 m sill height.
#
# extension 1-3
# Generate a 5-zone building model with identical rectangular zones sharing adjoining east-west walls. Each zone measures 7.0 m (length) × 4.9 m (width) × 7.5 m (height), featuring two 3.0 m (length) × 1.2 m (width) windows on both south and north walls with west edges offset 0.4 m and 3.8 m from wall west edges, all having 1.25 m sill height.
#
# extension 1-4
# Generate a 5-zone building model with identical rectangular zones sharing adjoining east-west walls. Each zone measures 7.0 m (length) × 4.9 m (width) × 7.5 m (height), featuring no windows on both south and north walls.
#
# Task 2 (original prompt)
# Generate a 10-zone building model with identical rectangular zones vertically stacked across 10 floors, sharing adjoining floor slabs. Each zone measures 13.4 m (length) × 9.1 m (width) × 7.8 m (height), featuring 8.4 m (length) × 3.2 m (width) windows on both south and north walls with west edges offset 1.1 m from wall west edges, both having 1.25 m sill height.
#
# extension 2-1
# Generate a 12-zone building model with identical rectangular zones vertically stacked across 12 floors, sharing adjoining floor slabs. Each zone measures 13.4 m (length) × 9.1 m (width) × 7.8 m (height), featuring 8.4 m (length) × 3.2 m (width) windows on both south and north walls with west edges offset 1.1 m from wall west edges, both having 1.25 m sill height.
#
# extension 2-2
# Generate a 8-zone building model with identical rectangular zones vertically stacked across 8 floors, sharing adjoining floor slabs. Each zone measures 13.4 m (length) × 9.1 m (width) × 7.8 m (height), featuring 8.4 m (length) × 3.2 m (width) windows on both south and north walls with west edges offset 1.1 m from wall west edges, both having 1.25 m sill height.
#
# extension 2-3
# Generate a 10-zone building model with identical rectangular zones vertically stacked across 10 floors, sharing adjoining floor slabs. Each zone measures 13.4 m (length) × 9.1 m (width) × 7.8 m (height), featuring two 4.4 m (length) × 3.2 m (width) windows on both south and north walls with west edges offset 1.1 m and 6.0 m from wall west edges, all having 1.25 m sill height.
#
# extension 2-4
# Generate a 10-zone building model with identical rectangular zones vertically stacked across 10 floors, sharing adjoining floor slabs. Each zone measures 13.4 m (length) × 9.1 m (width) × 7.8 m (height), featuring no windows on both south and north walls.
#
# Task 3 (original prompt)
# Generate a 10-zone building model comprising 2 identical floors with 5 zones each, sharing adjoining east-west walls. Each zone measures 10.2 m (length) × 8.3 m (width) × 7.4 m (height), featuring a 4.3 m (length) × 3.2 m (width) south window (west edge offset 1.2 m from wall west edge) and a 5.5 m (length) × 2.1 m (width) north window (west edge offset 0.6 m from wall west edge), both having 1.25 m sill height.
#
# extension 3-1
# Generate a 12-zone building model comprising 3 identical floors with 4 zones each, sharing adjoining east-west walls. Each zone measures 10.2 m (length) × 8.3 m (width) × 7.4 m (height), featuring a 4.3 m (length) × 3.2 m (width) south window (west edge offset 1.2 m from wall west edge) and a 5.5 m (length) × 2.1 m (width) north window (west edge offset 0.6 m from wall west edge), both having 1.25 m sill height.
#
# extension 3-2
# Generate a 6-zone building model comprising 2 identical floors with 3 zones each, sharing adjoining east-west walls. Each zone measures 10.2 m (length) × 8.3 m (width) × 7.4 m (height), featuring a 4.3 m (length) × 3.2 m (width) south window (west edge offset 1.2 m from wall west edge) and a 5.5 m (length) × 2.1 m (width) north window (west edge offset 0.6 m from wall west edge), both having 1.25 m sill height.
#
# extension 3-3
# Generate a 10-zone building model comprising 2 identical floors with 5 zones each, sharing adjoining east-west walls. Each zone measures 10.2 m (length) × 8.3 m (width) × 7.4 m (height), featuring two 3.3 m (length) × 3.2 m (width) south windows (west edge offset 1.2 m and 5.0 m from wall west edge) and two 4.5 m (length) × 2.1 m (width) north windows (west edge offset 0.5 m and 5.5 m from wall west edge), all having 1.25 m sill height.
#
# extension 3-4
# Generate a 10-zone building model comprising 2 identical floors with 5 zones each, sharing adjoining east-west walls. Each zone measures 10.2 m (length) × 8.3 m (width) × 7.4 m (height), featuring no south windows and no north windows.
#
#
# Task 4 (original prompt)
# Generate a 10-zone building model comprising 2 identical floors with 5 zones each, sharing 6.8 m (width) × 6.5 m (height) east-west walls but varying lengths (7.5/6.8/8.9/8.8/7.8 m west-to-east). Each zone contains two 2.3 m (length) × 2.0 m (width) south windows (west edges offset 0.4 m and 3.4 m from wall west edge) and two 2.3 m (length) × 2.2 m (width) north windows (west edges offset 0.2 m and 3.1 m from wall west edge), all having 1.25 m sill height.
#
# extension 4-1
# Generate a 12-zone building model comprising 3 identical floors with 4 zones each, sharing 6.8 m (width) × 6.5 m (height) east-west walls but varying lengths (7.5/6.8/8.9/8.8 m west-to-east). Each zone contains two 2.3 m (length) × 2.0 m (width) south windows (west edges offset 0.4 m and 3.4 m from wall west edge) and two 2.3 m (length) × 2.2 m (width) north windows (west edges offset 0.2 m and 3.1 m from wall west edge), all having 1.25 m sill height.
#
# extension 4-2
# Generate a 6-zone building model comprising 2 identical floors with 3 zones each, sharing 6.8 m (width) × 6.5 m (height) east-west walls but varying lengths (7.5/6.8/8.9 m west-to-east). Each zone contains two 2.3 m (length) × 2.0 m (width) south windows (west edges offset 0.4 m and 3.4 m from wall west edge) and two 2.3 m (length) × 2.2 m (width) north windows (west edges offset 0.2 m and 3.1 m from wall west edge), all having 1.25 m sill height.
#
# extension 4-3
# Generate a 10-zone building model comprising 2 identical floors with 5 zones each, sharing 6.8 m (width) × 6.5 m (height) east-west walls but varying lengths (7.5/6.8/8.9/8.8/7.8 m west-to-east). Each zone contains three 1.3 m (length) × 2.0 m (width) south windows (west edges offset 0.4 m, 2.0 m and 3.6 m from wall west edge) and three 1.3 m (length) × 2.2 m (width) north windows (west edges offset 0.2 m, 1.9 m and 3.8 m from wall west edge), all having 1.25 m sill height.
#
# extension 4-4
# Generate a 10-zone building model comprising 2 identical floors with 5 zones each, sharing 6.8 m (width) × 6.5 m (height) east-west walls but varying lengths (7.5/6.8/8.9/8.8/7.8 m west-to-east). Each zone contains one 2.3 m (length) × 2.0 m (width) south window (west edges offset 0.4 m from wall west edge) and one 2.3 m (length) × 2.2 m (width) north window (west edges offset 0.2 m from wall west edge), all having 1.25 m sill height.
#
