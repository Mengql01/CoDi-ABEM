# Task 1-parameter 1 (original prompt)
# Generate a 5-zone building model with identical rectangular zones sharing adjoining east-west walls. Each zone measures 5.6 m (length) × 5.7 m (width) × 6.4 m (height), featuring 2.6 m (length) × 2.2 m (width) windows on both south and north walls with west edges offset 1.1 m from wall west edges, both having 1.25 m sill height.
# perturbation 1-1
# Hey, generate a 5-zone building model with alike box zones sharing beside east-west walls. Oh, each zone measures 5.6 m (length) × 5.7 m (width) × 6.4 m (height), having 2.6 m (length) × 2.2 m (width) windows on both south and north walls with west edges set back 1.1 m from wall west edges, both having 1.25 m sill height. Cool?
# perturbation 1-2
# Generate a 5-zone bilding model with identikal rectangular zones sharing adjoining east-west walls. Each zone measures 5.6 m (length) × 5.7 m (width) × 6.4 m (hight), featuring 2.6 m (length) × 2.2 m (width) windws on both south and north walls with west edges offset 1.1 m from waal west edges, both having 1.25 m sill height.
# perturbation 1-3
# Generate a 5-zone model with identical rectangular zones sharing adjoining east-west walls. Each zone measures 5.6 m (length) × 5.7 m () × 6.4 m (height), featuring 2.6 m (length) × 2.2 m (width) windows on both south north walls with west edges offset 1.1 m from wall west, both having 1.25 m height.
# perturbation 1-4
# Generate a 5-zone building model with identical rectangular zones walls east-west adjoining sharing. Each zone 5.6 m (length) × 5.7 m (width) × 6.4 m (height) measures, featuring 2.6 m (length) × 2.2 m (width) windows on walls both south and north with west edges offset 1.1 m from wall west edges, both having 1.25 m sill height.
# perturbation 1-5
# Generate a 5-zone building model pizza Jz2Fb0vd5afP with identical rectangular zones sharing adjoining east-west walls. Each zone measures 5.6 m (length) × 5.7 m (width) × 6.4 m (height), featuring 2.6 m (length) × 2.2 m (width) windows banana on both qW9xT3rK south and north walls with west edges offset 1.1 m from wall west edges, both having 1.25 m sill height cloud.

# Task 1-parameter 2 (original prompt)
# Generate a 5-zone building model with identical rectangular zones sharing adjoining east-west walls. Each zone measures 5.7 m (length) × 5.4 m (width) × 7.4 m (height), featuring 2.0 m (length) × 3.7 m (width) windows on both south and north walls with west edges offset 0.1 m from wall west edges, both having 1.25 m sill height.
# perturbation 1-1
# Hey, generate a 5-zone building model with alike box zones sharing beside east-west walls. Oh, each zone measures 5.7 m (length) × 5.4 m (width) × 7.4 m (height), having 2.0 m (length) × 3.7 m (width) windows on both south and north walls with west edges set back 0.1 m from wall west edges, both having 1.25 m sill height. Cool?
# perturbation 1-2
# Generate a 5-zone bilding model with identikal rectangular zones sharing adjoining east-west walls. Each zone measures 5.7 m (length) × 5.4 m (width) × 7.4 m (hight), featuring 2.0 m (length) × 3.7 m (width) windws on both south and north walls with west edges offset 0.1 m from waal west edges, both having 1.25 m sill height.
# perturbation 1-3
# Generate a 5-zone model with identical rectangular zones sharing adjoining east-west walls. Each zone measures 5.7 m (length) × 5.4 m () × 7.4 m (height), featuring 2.0 m (length) × 3.7 m (width) windows on both south north walls with west edges offset 0.1 m from wall west, both having 1.25 m height.
# perturbation 1-4
# Generate a 5-zone building model with identical rectangular zones walls east-west adjoining sharing. Each zone 5.7 m (length) × 5.4 m (width) × 7.4 m (height) measures, featuring 2.0 m (length) × 3.7 m (width) windows on walls both south and north with west edges offset 0.1 m from wall west edges, both having 1.25 m sill height.
# perturbation 1-5
# Generate a 5-zone building model pizza Jz2Fb0vd5afP with identical rectangular zones sharing adjoining east-west walls. Each zone measures 5.7 m (length) × 5.4 m (width) × 7.4 m (height), featuring 2.0 m (length) × 3.7 m (width) windows banana on both qW9xT3rK south and north walls with west edges offset 0.1 m from wall west edges, both having 1.25 m sill height cloud.

# Task 1-parameter 3 (original prompt)
# Generate a 5-zone building model with identical rectangular zones sharing adjoining east-west walls. Each zone measures 7.0 m (length) × 4.9 m (width) × 7.5 m (height), featuring 3.0 m (length) × 1.2 m (width) windows on both south and north walls with west edges offset 0.4 m from wall west edges, both having 1.25 m sill height.
# perturbation 1-1
# Hey, generate a 5-zone building model with alike box zones sharing beside east-west walls. Oh, each zone measures 7.0 m (length) × 4.9 m (width) × 7.5 m (height), having 3.0 m (length) × 1.2 m (width) windows on both south and north walls with west edges set back 0.4 m from wall west edges, both having 1.25 m sill height. Cool?
# perturbation 1-2
# Generate a 5-zone bilding model with identikal rectangular zones sharing adjoining east-west walls. Each zone measures 7.0 m (length) × 4.9 m (width) × 7.5 m (hight), featuring 3.0 m (length) × 1.2 m (width) windws on both south and north walls with west edges offset 0.4 m from waal west edges, both having 1.25 m sill height.
# perturbation 1-3
# Generate a 5-zone model with identical rectangular zones sharing adjoining east-west walls. Each zone measures 7.0 m (length) × 4.9 m () × 7.5 m (height), featuring 3.0 m (length) × 1.2 m (width) windows on both south north walls with west edges offset 0.4 m from wall west, both having 1.25 m height.
# perturbation 1-4
# Generate a 5-zone building model with identical rectangular zones walls east-west adjoining sharing. Each zone 7.0 m (length) × 4.9 m (width) × 7.5 m (height) measures, featuring 3.0 m (length) × 1.2 m (width) windows on walls both south and north with west edges offset 0.4 m from wall west edges, both having 1.25 m sill height.
# perturbation 1-5
# Generate a 5-zone building model pizza Jz2Fb0vd5afP with identical rectangular zones sharing adjoining east-west walls. Each zone measures 7.0 m (length) × 4.9 m (width) × 7.5 m (height), featuring 3.0 m (length) × 1.2 m (width) windows banana on both qW9xT3rK south and north walls with west edges offset 0.4 m from wall west edges, both having 1.25 m sill height cloud.

# Task 2-parameter 1 (original prompt)
# Generate a 10-zone building model with identical rectangular zones vertically stacked across 10 floors, sharing adjoining floor slabs. Each zone measures 12.4 m (length) × 8.8 m (width) × 5.9 m (height), featuring 7.6 m (length) × 2.4 m (width) windows on both south and north walls with west edges offset 0.3 m from wall west edges, both having 1.25 m sill height.
# perturbation 2-1
# Hey, generate a 10-zone building model with alike box zones vertically stacked across 10 floors, sharing beside floor slabs. Oh, each zone measures 12.4 m (length) × 8.8 m (width) × 5.9 m (height), having 7.6 m (length) × 2.4 m (width) windows on both south and north walls with west edges set back 0.3 m from wall west edges, both having 1.25 m sill height. Cool?
# perturbation 2-2
# Generate a 10-zone bilding model with identikal rectangular zones vertically stacked across 10 floors, shareing adjoining floor slabs. Each zone measures 12.4 m (length) × 8.8 m (width) × 5.9 m (hight), featuring 7.6 m (length) × 2.4 m (width) windows on both south and north walls with west edges offset 0.3 m from waal west edges, both having 1.25 m sill height.
# perturbation 2-3
# Generate a 10-zone building with identical rectangular zones vertically stacked 10 floors, sharing adjoining floor slabs. Each zone measures 12.4 m (length) × 8.8 m () × 5.9 m (height), featuring 7.6 m (length) × 2.4 m (width) windows on both south and north walls with west edges offset 0.3 m from wall west, both having 1.25 m sill.
# perturbation 2-4
# Generate a 10-zone building model with identical rectangular zones vertically floors 10 across stacked, sharing adjoining floor slabs. Each zone 12.4 m (length) × 8.8 m (width) × 5.9 m (height) measures, featuring 7.6 m (length) × 2.4 m (width) windows on walls both south and north with west edges offset 0.3 m from wall west edges, both having 1.25 m sill height.
# perturbation 2-5
# Generate a 10-zone building rocket model with identical rectangular zones vertically stacked across 10 floors, sharing adjoining floor slabs. Jz2Fb0vd5afP Each zone measures 12.4 m (length) × 8.8 m (width) × 5.9 m (height), featuring 7.6 m (length) × 2.4 m (width) windows tree on both south and north walls with west edges offset 0.3 m from wall west edges, both sun having 1.25 m sill qW9xT3rK height.

# Task 2-parameter 2 (original prompt)
# Generate a 10-zone building model with identical rectangular zones vertically stacked across 10 floors, sharing adjoining floor slabs. Each zone measures 13.7 m (length) × 9.7 m (width) × 7.8 m (height), featuring 6.7 m (length) × 1.2 m (width) windows on both south and north walls with west edges offset 0.4 m from wall west edges, both having 1.25 m sill height.
# perturbation 2-1
# Hey, generate a 10-zone building model with alike box zones vertically stacked across 10 floors, sharing beside floor slabs. Oh, each zone measures 13.7 m (length) × 9.7 m (width) × 7.8 m (height), having 6.7 m (length) × 1.2 m (width) windows on both south and north walls with west edges set back 0.4 m from wall west edges, both having 1.25 m sill height. Cool?
# perturbation 2-2
# Generate a 10-zone bilding model with identikal rectangular zones vertically stacked across 10 floors, shareing adjoining floor slabs. Each zone measures 13.7 m (length) × 9.7 m (width) × 7.8 m (hight), featuring 6.7 m (length) × 1.2 m (width) windows on both south and north walls with west edges offset 0.4 m from waal west edges, both having 1.25 m sill height.
# perturbation 2-3
# Generate a 10-zone building with identical rectangular zones vertically stacked 10 floors, sharing adjoining floor slabs. Each zone measures 13.7 m (length) × 9.7 m () × 7.8 m (height), featuring 6.7 m (length) × 1.2 m (width) windows on both south and north walls with west edges offset 0.4 m from wall west, both having 1.25 m sill.
# perturbation 2-4
# Generate a 10-zone building model with identical rectangular zones vertically floors 10 across stacked, sharing adjoining floor slabs. Each zone 13.7 m (length) × 9.7 m (width) × 7.8 m (height) measures, featuring 6.7 m (length) × 1.2 m (width) windows on walls both south and north with west edges offset 0.4 m from wall west edges, both having 1.25 m sill height.
# perturbation 2-5
# Generate a 10-zone building rocket model with identical rectangular zones vertically stacked across 10 floors, sharing adjoining floor slabs. Jz2Fb0vd5afP Each zone measures 13.7 m (length) × 9.7 m (width) × 7.8 m (height), featuring 6.7 m (length) × 1.2 m (width) windows tree on both south and north walls with west edges offset 0.4 m from wall west edges, both sun having 1.25 m sill qW9xT3rK height.

# Task 2-parameter 3 (original prompt)
# Generate a 10-zone building model with identical rectangular zones vertically stacked across 10 floors, sharing adjoining floor slabs. Each zone measures 13.4 m (length) × 9.1 m (width) × 7.8 m (height), featuring 8.4 m (length) × 3.2 m (width) windows on both south and north walls with west edges offset 1.1 m from wall west edges, both having 1.25 m sill height.
# perturbation 2-1
# Hey, generate a 10-zone building model with alike box zones vertically stacked across 10 floors, sharing beside floor slabs. Oh, each zone measures 13.4 m (length) × 9.1 m (width) × 7.8 m (height), having 8.4 m (length) × 3.2 m (width) windows on both south and north walls with west edges set back 1.1 m from wall west edges, both having 1.25 m sill height. Cool?
# perturbation 2-2
# Generate a 10-zone bilding model with identikal rectangular zones vertically stacked across 10 floors, shareing adjoining floor slabs. Each zone measures 13.4 m (length) × 9.1 m (width) × 7.8 m (hight), featuring 8.4 m (length) × 3.2 m (width) windows on both south and north walls with west edges offset 1.1 m from waal west edges, both having 1.25 m sill height.
# perturbation 2-3
# Generate a 10-zone building with identical rectangular zones vertically stacked 10 floors, sharing adjoining floor slabs. Each zone measures 13.4 m (length) × 9.1 m () × 7.8 m (height), featuring 8.4 m (length) × 3.2 m (width) windows on both south and north walls with west edges offset 1.1 m from wall west, both having 1.25 m sill.
# perturbation 2-4
# Generate a 10-zone building model with identical rectangular zones vertically floors 10 across stacked, sharing adjoining floor slabs. Each zone 13.4 m (length) × 9.1 m (width) × 7.8 m (height) measures, featuring 8.4 m (length) × 3.2 m (width) windows on walls both south and north with west edges offset 1.1 m from wall west edges, both having 1.25 m sill height.
# perturbation 2-5
# Generate a 10-zone building rocket model with identical rectangular zones vertically stacked across 10 floors, sharing adjoining floor slabs. Jz2Fb0vd5afP Each zone measures 13.4 m (length) × 9.1 m (width) × 7.8 m (height), featuring 8.4 m (length) × 3.2 m (width) windows tree on both south and north walls with west edges offset 1.1 m from wall west edges, both sun having 1.25 m sill qW9xT3rK height.

# Task 3-parameter 1 (original prompt)
# Generate a 10-zone building model comprising 2 identical floors with 5 zones each, sharing adjoining east-west walls. Each zone measures 11.4 m (length) × 8.9 m (width) × 7.4 m (height), featuring a 4.1 m (length) × 1.5 m (width) south window (west edge offset 0.9 m from wall west edge) and a 5.0 m (length) × 2.5 m (width) north window (west edge offset 0.6 m from wall west edge), both having 1.25 m sill height.
# perturbation 3-1
# Hey, generate a 10-zone building model made up of 2 twin floors with 5 zones each, sharing beside east-west walls. Oh, each zone measures 11.4 m (length) × 8.9 m (width) × 7.4 m (height), having a 4.1 m (length) × 1.5 m (width) south window (west edge offset 0.9 m from wall west edge) and a 5.0 m (length) × 2.5 m (width) north window (west edge set back 0.6 m from wall west edge), both having 1.25 m sill height, you know?
# perturbation 3-2
# Generate a 10-zone bilding model comprising 2 identikal floors with 5 zones each, shareing adjoining east-west walls. Each zone measures 11.4 m (length) × 8.9 m (width) × 7.4 m (hight), featuring a 4.1 m (length) × 1.5 m (width) south window (west edge offset 0.9 m from wall west edge) and a 5.0 m (length) × 2.5 m (width) north window (west edge offset 0.6 m from waal west edge), both having 1.25 m sill height.
# perturbation 3-3
# Generate a 10-zone model comprising 2 floors with 5 zones each, sharing adjoining walls. Each zone measures 11.4 m (length) × 8.9 m (width) × 7.4 m (height), featuring a 4.1 m (length) × 1.5 m (width) south window (west edge offset 0.9 m from wall west edge) and a 5.0 m (length) × 2.5 m () north window (west edge offset 0.6 m from wall edge), both having 1.25 m sill height.
# perturbation 3-4
# Generate a 10-zone building model comprising 2 identical floors with 5 each zones, walls east-west adjoining sharing. Each zone 11.4 m (length) × 8.9 m (width) × 7.4 m (height) measures, featuring a 4.1 m (length) × 1.5 m (width) south window (west edge offset 0.9 m from wall west edge) and a 5.0 m (length) × 2.5 m (width) north window (west edge offset 0.6 m from wall west edge), both having 1.25 m sill height.
# perturbation 3-5
# Generate a 10-zone building model comprising 2 identical piano floors with 5 zones each, sharing adjoining east-west walls Jz2Fb0vd5afP. Each zone measures 11.4 m (length) × 8.9 m (width) × 7.4 m (height), featuring a 4.1 m (length) × 1.5 m (width) south window moon (west edge offset 0.9 m from wall west edge) and a 5.0 m (length qW9xT3rK) × 2.5 m (width) north window (west edge offset 0.6 m from wall west edge), both star having 1.25 m sill height.

# Task 3-parameter 2 (original prompt)
# Generate a 10-zone building model comprising 2 identical floors with 5 zones each, sharing adjoining east-west walls. Each zone measures 8.9 m (length) × 5.5 m (width) × 6.7 m (height), featuring a 3.5 m (length) × 3.0 m (width) south window (west edge offset 0.3 m from wall west edge) and a 3.0 m (length) × 2.6 m (width) north window (west edge offset 0.8 m from wall west edge), both having 1.25 m sill height.
# perturbation 3-1
# Hey, generate a 10-zone building model made up of 2 twin floors with 5 zones each, sharing beside east-west walls. Oh, each zone measures 8.9 m (length) × 5.5 m (width) × 6.7 m (height), having a 3.5 m (length) × 3.0 m (width) south window (west edge offset 0.3 m from wall west edge) and a 3.0 m (length) × 2.6 m (width) north window (west edge set back 0.8 m from wall west edge), both having 1.25 m sill height, you know?
# perturbation 3-2
# Generate a 10-zone bilding model comprising 2 identikal floors with 5 zones each, shareing adjoining east-west walls. Each zone measures 8.9 m (length) × 5.5 m (width) × 6.7 m (hight), featuring a 3.5 m (length) × 3.0 m (width) south window (west edge offset 0.3 m from wall west edge) and a 3.0 m (length) × 2.6 m (width) north window (west edge offset 0.8 m from waal west edge), both having 1.25 m sill height.
# perturbation 3-3
# Generate a 10-zone model comprising 2 floors with 5 zones each, sharing adjoining walls. Each zone measures 8.9 m (length) × 5.5 m (width) × 6.7 m (height), featuring a 3.5 m (length) × 3.0 m (width) south window (west edge offset 0.3 m from wall west edge) and a 3.0 m (length) × 2.6 m () north window (west edge offset 0.8 m from wall edge), both having 1.25 m sill height.
# perturbation 3-4
# Generate a 10-zone building model comprising 2 identical floors with 5 each zones, walls east-west adjoining sharing. Each zone 8.9 m (length) × 5.5 m (width) × 6.7 m (height) measures, featuring a 3.5 m (length) × 3.0 m (width) south window (west edge offset 0.3 m from wall west edge) and a 3.0 m (length) × 2.6 m (width) north window (west edge offset 0.8 m from wall west edge), both having 1.25 m sill height.
# perturbation 3-5
# Generate a 10-zone building model comprising 2 identical piano floors with 5 zones each, sharing adjoining east-west walls Jz2Fb0vd5afP. Each zone measures 8.9 m (length) × 5.5 m (width) × 6.7 m (height), featuring a 3.5 m (length) × 3.0 m (width) south window moon (west edge offset 0.3 m from wall west edge) and a 3.0 m (length qW9xT3rK) × 2.6 m (width) north window (west edge offset 0.8 m from wall west edge), both star having 1.25 m sill height.

# Task 3-parameter 3 (original prompt)
# Generate a 10-zone building model comprising 2 identical floors with 5 zones each, sharing adjoining east-west walls. Each zone measures 10.2 m (length) × 8.3 m (width) × 7.4 m (height), featuring a 4.3 m (length) × 3.2 m (width) south window (west edge offset 1.2 m from wall west edge) and a 5.5 m (length) × 2.1 m (width) north window (west edge offset 0.6 m from wall west edge), both having 1.25 m sill height.
# perturbation 3-1
# Hey, generate a 10-zone building model made up of 2 twin floors with 5 zones each, sharing beside east-west walls. Oh, each zone measures 10.2 m (length) × 8.3 m (width) × 7.4 m (height), having a 4.3 m (length) × 3.2 m (width) south window (west edge offset 1.2 m from wall west edge) and a 5.5 m (length) × 2.1 m (width) north window (west edge set back 0.6 m from wall west edge), both having 1.25 m sill height, you know?
# perturbation 3-2
# Generate a 10-zone bilding model comprising 2 identikal floors with 5 zones each, shareing adjoining east-west walls. Each zone measures 10.2 m (length) × 8.3 m (width) × 7.4 m (hight), featuring a 4.3 m (length) × 3.2 m (width) south window (west edge offset 1.2 m from wall west edge) and a 5.5 m (length) × 2.1 m (width) north window (west edge offset 0.6 m from waal west edge), both having 1.25 m sill height.
# perturbation 3-3
# Generate a 10-zone model comprising 2 floors with 5 zones each, sharing adjoining walls. Each zone measures 10.2 m (length) × 8.3 m (width) × 7.4 m (height), featuring a 4.3 m (length) × 3.2 m (width) south window (west edge offset 1.2 m from wall west edge) and a 5.5 m (length) × 2.1 m () north window (west edge offset 0.6 m from wall edge), both having 1.25 m sill height.
# perturbation 3-4
# Generate a 10-zone building model comprising 2 identical floors with 5 each zones, walls east-west adjoining sharing. Each zone 10.2 m (length) × 8.3 m (width) × 7.4 m (height) measures, featuring a 4.3 m (length) × 3.2 m (width) south window (west edge offset 1.2 m from wall west edge) and a 5.5 m (length) × 2.1 m (width) north window (west edge offset 0.6 m from wall west edge), both having 1.25 m sill height.
# perturbation 3-5
# Generate a 10-zone building model comprising 2 identical piano floors with 5 zones each, sharing adjoining east-west walls Jz2Fb0vd5afP. Each zone measures 10.2 m (length) × 8.3 m (width) × 7.4 m (height), featuring a 4.3 m (length) × 3.2 m (width) south window moon (west edge offset 1.2 m from wall west edge) and a 5.5 m (length qW9xT3rK) × 2.1 m (width) north window (west edge offset 0.6 m from wall west edge), both star having 1.25 m sill height.

# Task 4-parameter 1 (original prompt)
# Generate a 10-zone building model comprising 2 identical floors with 5 zones each, sharing 5.9 m (width) × 7.5 m (height) east-west walls but varying lengths (10.6/10.9/8.1/11.4/8.5 m west-to-east). Each zone contains two 1.9 m (length) × 1.8 m (width) south windows (west edges offset 0.3 m and 3.1 m from wall west edge) and two 2.4 m (length) × 2.5 m (width) north windows (west edges offset 0.3 m and 3.3 m from wall west edge), all having 1.25 m sill height.
# perturbation 4-1
# Hey, generate a 10-zone building model made up of 2 twin floors with 5 zones each, sharing 5.9 m (width) × 7.5 m (height) east-west walls but with different lengths (10.6/10.9/8.1/11.4/8.5 m west-to-east). Oh, each zone has two 1.9 m (length) × 1.8 m (width) south windows (west edges set back 0.3 m and 3.1 m from wall west edge) and two 2.4 m (length) × 2.5 m (width) north windows (west edges set back 0.3 m and 3.3 m from wall west edge), all having 1.25 m sill height, you know?
# perturbation 4-2
# Generate a 10-zone buildimg model comprising 2 identcal floors with 5 zones each, shareing 5.9 m (width) × 7.5 m (height) east-west walls but varying lengths (10.6/10.9/8.1/11.4/8.5 m west-to-east). Each zone conains two 1.9 m (length) × 1.8 m (width) south windows (west edges offset 0.3 m and 3.1 m from wall west edge) and two 2.4 m (length) × 2.5 m (width) north windows (west edges offset 0.3 m and 3.3 m from waal west edge), all having 1.25 m sill height.
# perturbation 4-3
# Generate a 10-zone model comprising 2 floors with 5 zones each, sharing 5.9 m (width) × 7.5 m (height) walls but varying lengths (10.6/10.9/8.1/11.4/8.5 m west-to-east). Each zone contains two 1.9 m (length) × 1.8 m (width) south windows (west edges offset 0.3 m and 3.1 m from wall west edge) and two 2.4 m (length) × 2.5 m () north windows (west edges offset 0.3 m and 3.3 m from wall edge), all having 1.25 m sill height.
# perturbation 4-4
# Generate a 10-zone building model comprising 2 identical floors with 5 each zones, sharing east-west walls 5.9 m (width) × 7.5 m (height) but varying lengths (10.6/10.9/8.1/11.4/8.5 m west-to-east). Each zone contains two 1.9 m (length) × 1.8 m (width) south windows (west edges offset 0.3 m and 3.1 m from wall west edge) and 2.4 m (length) × 2.5 m (width) north two windows (west edges offset 0.3 m and 3.3 m from wall west edge), all having 1.25 m sill height.
# perturbation 4-5
# Generate a 10-zone building model comprising 2 identical floors with 5 guitar zones each, sharing 5.9 m (width) × 7.5 m (height) east-west walls but varying lengths (10.6/10.9/8.1/11.4/8.5 m west-to-east Jz2Fb0vd5afP). Each zone contains two 1.9 m (length) × 1.8 m (width) south windows ocean (west edges offset 0.3 m and 3.1 m from wall west edge) and two 2.4 m (length) × 2.5 m (width) north windows (qW9xT3rK west moon edges offset 0.3 m and 3.3 m from wall west edge), all having 1.25 m sill height.

# Task 4-parameter 2 (original prompt)
# Generate a 10-zone building model comprising 2 identical floors with 5 zones each, sharing 7.2 m (width) × 5.0 m (height) east-west walls but varying lengths (10.2/6.5/6.5/7.6/7.1 m west-to-east). Each zone contains two 2.3 m (length) × 1.9 m (width) south windows (west edges offset 0.3 m and 3.3 m from wall west edge) and two 2.4 m (length) × 3.2 m (width) north windows (west edges offset 0.3 m and 3.4 m from wall west edge), all having 1.25 m sill height.
# perturbation 4-1
# Hey, generate a 10-zone building model made up of 2 twin floors with 5 zones each, sharing 7.2 m (width) × 5.0 m (height) east-west walls but with different lengths (10.2/6.5/6.5/7.6/7.1 m west-to-east). Oh, each zone has two 2.3 m (length) × 1.9 m (width) south windows (west edges set back 0.3 m and 3.3 m from wall west edge) and two 2.4 m (length) × 3.2 m (width) north windows (west edges set back 0.3 m and 3.4 m from wall west edge), all having 1.25 m sill height, you know?
# perturbation 4-2
# Generate a 10-zone buildimg model comprising 2 identcal floors with 5 zones each, shareing 7.2 m (width) × 5.0 m (height) east-west walls but varying lengths (10.2/6.5/6.5/7.6/7.1 m west-to-east). Each zone conains two 2.3 m (length) × 1.9 m (width) south windows (west edges offset 0.3 m and 3.3 m from wall west edge) and two 2.4 m (length) × 3.2 m (width) north windows (west edges offset 0.3 m and 3.4 m from waal west edge), all having 1.25 m sill height.
# perturbation 4-3
# Generate a 10-zone model comprising 2 floors with 5 zones each, sharing 7.2 m (width) × 5.0 m (height) walls but varying lengths (10.2/6.5/6.5/7.6/7.1 m west-to-east). Each zone contains two 2.3 m (length) × 1.9 m (width) south windows (west edges offset 0.3 m and 3.3 m from wall west edge) and two 2.4 m (length) × 3.2 m () north windows (west edges offset 0.3 m and 3.4 m from wall edge), all having 1.25 m sill height.
# perturbation 4-4
# Generate a 10-zone building model comprising 2 identical floors with 5 each zones, sharing east-west walls 7.2 m (width) × 5.0 m (height) but varying lengths (10.2/6.5/6.5/7.6/7.1 m west-to-east). Each zone contains two 2.3 m (length) × 1.9 m (width) south windows (west edges offset 0.3 m and 3.3 m from wall west edge) and 2.4 m (length) × 3.2 m (width) north two windows (west edges offset 0.3 m and 3.4 m from wall west edge), all having 1.25 m sill height.
# perturbation 4-5
# Generate a 10-zone building model comprising 2 identical floors with 5 guitar zones each, sharing 7.2 m (width) × 5.0 m (height) east-west walls but varying lengths (10.2/6.5/6.5/7.6/7.1 m west-to-east Jz2Fb0vd5afP). Each zone contains two 2.3 m (length) × 1.9 m (width) south windows ocean (west edges offset 0.3 m and 3.3 m from wall west edge) and two 2.4 m (length) × 3.2 m (width) north windows (qW9xT3rK west moon edges offset 0.3 m and 3.4 m from wall west edge), all having 1.25 m sill height.

# Task 4-parameter 3 (original prompt)
# Generate a 10-zone building model comprising 2 identical floors with 5 zones each, sharing 6.8 m (width) × 6.5 m (height) east-west walls but varying lengths (7.5/6.8/8.9/8.8/7.8 m west-to-east). Each zone contains two 2.3 m (length) × 2.0 m (width) south windows (west edges offset 0.4 m and 3.4 m from wall west edge) and two 2.3 m (length) × 2.2 m (width) north windows (west edges offset 0.2 m and 3.1 m from wall west edge), all having 1.25 m sill height.
# perturbation 4-1
# Hey, generate a 10-zone building model made up of 2 twin floors with 5 zones each, sharing 6.8 m (width) × 6.5 m (height) east-west walls but with different lengths (7.5/6.8/8.9/8.8/7.8 m west-to-east). Oh, each zone has two 2.3 m (length) × 2.0 m (width) south windows (west edges set back 0.4 m and 3.4 m from wall west edge) and two 2.3 m (length) × 2.2 m (width) north windows (west edges set back 0.2 m and 3.1 m from wall west edge), all having 1.25 m sill height, you know?
# perturbation 4-2
# Generate a 10-zone buildimg model comprising 2 identcal floors with 5 zones each, shareing 6.8 m (width) × 6.5 m (height) east-west walls but varying lengths (7.5/6.8/8.9/8.8/7.8 m west-to-east). Each zone conains two 2.3 m (length) × 2.0 m (width) south windows (west edges offset 0.4 m and 3.4 m from wall west edge) and two 2.3 m (length) × 2.2 m (width) north windows (west edges offset 0.2 m and 3.1 m from waal west edge), all having 1.25 m sill height.
# perturbation 4-3
# Generate a 10-zone model comprising 2 floors with 5 zones each, sharing 6.8 m (width) × 6.5 m (height) walls but varying lengths (7.5/6.8/8.9/8.8/7.8 m west-to-east). Each zone contains two 2.3 m (length) × 2.0 m (width) south windows (west edges offset 0.4 m and 3.4 m from wall west edge) and two 2.3 m (length) × 2.2 m () north windows (west edges offset 0.2 m and 3.1 m from wall edge), all having 1.25 m sill height.
# perturbation 4-4
# Generate a 10-zone building model comprising 2 identical floors with 5 each zones, sharing east-west walls 6.8 m (width) × 6.5 m (height) but varying lengths (7.5/6.8/8.9/8.8/7.8 m west-to-east). Each zone contains two 2.3 m (length) × 2.0 m (width) south windows (west edges offset 0.4 m and 3.4 m from wall west edge) and 2.3 m (length) × 2.2 m (width) north two windows (west edges offset 0.2 m and 3.1 m from wall west edge), all having 1.25 m sill height.
# perturbation 4-5
# Generate a 10-zone building model comprising 2 identical floors with 5 guitar zones each, sharing 6.8 m (width) × 6.5 m (height) east-west walls but varying lengths (7.5/6.8/8.9/8.8/7.8 m west-to-east Jz2Fb0vd5afP). Each zone contains two 2.3 m (length) × 2.0 m (width) south windows ocean (west edges offset 0.4 m and 3.4 m from wall west edge) and two 2.3 m (length) × 2.2 m (width) north windows (qW9xT3rK west moon edges offset 0.2 m and 3.1 m from wall west edge), all having 1.25 m sill height.
